import React, { useState } from 'react';

const DarkMode = () => {

    const [light, setLight] = useState(0);

    }

    return (
        <div>
            <button onClick={}>Lune</button>
        </div>
    )
}

export default DarkMode;